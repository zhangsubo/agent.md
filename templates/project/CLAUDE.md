@AGENTS.md

## Claude Code Adapter

- 本文件只作为 Claude Code 的项目入口，通用规则统一维护在 `AGENTS.md`。
- Claude Code 专属内容可以写在这里，例如 `@path` 引用、hook 说明、Claude Code 的本地读取机制。
- 不要在这里重复项目介绍、技术栈、构建命令、代码规范等通用内容；这些内容应写入 `AGENTS.md` 或 `Reference_myself/`。
